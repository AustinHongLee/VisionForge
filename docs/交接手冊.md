# VisionForge 架構代理交接手冊

| 項目 | 內容 |
|---|---|
| 狀態 | 🟩 定案・活文件（每次交接前由卸任者更新） |
| 角色 | 架構代理 Architect（分工協議 v1.2：綁角色不綁型號） |
| 本次交接 | Claude Fable 5 → Claude Opus 4.8（已就任） |
| 交接時點 | 2026-07-07，commit 基準：儲存層完成（ADR-0005） |
| 就任後更新 | 2026-07-08，Opus 4.8：票-0003/0004 皆合併（4ab83f7／7ef91be）；決策者定調「並行雙軌＋批次發票」；發出批次 L0（票-0005/0006），Architect 親手起 kernel 校準引擎（ADR-0007） |

## 零、換電腦快速規則（給李宗鴻）

你在這個專案的預設角色是**快遞＋蓋章**，不是技術判斷者。換到另一台電腦時，先做：

```powershell
git clone https://github.com/AustinHongLee/VisionForge.git
cd VisionForge
git status --short --branch
```

接著把本手冊、`docs/README.md`、最新規格票貼給當前 Builder／Architect。除非你主動想看實作，否則只做三件事：

1. 跑 Architect 給你的 git 指令。
2. 把 Architect 發出的規格票原文轉貼給 Codex／Builder。
3. Codex 回報 L0 票 CI 綠時，跑 Architect 或 Codex 給的合併指令。

任何「要不要這樣設計」、「這個狀態是否正確」、「某票能不能合併」都不該丟給你判斷。Architect 自己查，Builder 自己施工，CI 自己守門。

## 一、就任程序（按順序，不可跳）

1. 讀 `docs/00-法規/` 全部——憲法 79 條與分工協議。你受 D19／D20 約束，PR 說明必須聲明憲法合規。
2. 讀 `docs/README.md`——文件狀態板，分清楚哪些算數（🟩）、哪些是歷史（🟧⬛）。
3. 讀本手冊第三～五節。
4. 回報「已就緒」＋你認為的下一步，等決策者（李宗鴻）一句話確認後動工。

## 二、你的職責（協議摘要）

Kernel 設計與實作（contracts／storage／orchestrator／統計）、L2 審查、發規格票、ADR 起草、治理維護。Builder（Codex）做 userland；發不出規格票的工作就是你該親手做的工作。

## 三、目前戰況

**已完成**：治理文件體系（憲法／協議／ADR-0001~0006，其中 0004/0005/0006 已隨程式碼合併轉 🟩 已採納）、Claim Schema、帳本 Schema、儲存層（append-only repos＋遷移＋blob 庫）、票-0001/0002/0003 皆已合併（票-0003＝匯入管線，commit 4ab83f7）。core＋app 本地驗證 66 passed、ruff 乾淨、D1/D13 守門通過、UI test/typecheck/lint/depcruise/build 皆綠。

**執行模式（決策者 2026-07-08 定調）**：**並行雙軌＋批次發票**。Architect 與 Codex 同時推進不輪流：Architect 跑 kernel（`core/`），Codex 跑 userland（`app/`、`ui/`、`providers/`）。userland 一律 L0，CI 綠由決策者直接自併、**不回 Architect**；只有 L2／kernel／契約變更才同步。發票採批次（一次 2–3 張排隊），減少人工轉手。

**L1 抽審計數（§3-2）**：抽樣率已放寬至每 5 張抽 1（Codex 連續零瑕疵）。自票-0006 抽查後又累積多張 L0（票-0007/0008/0009/0012/0013/0014/0015…），**下次抽一張補上**（Architect 近程；Codex 迄今零瑕疵、且數次主動抓到 Architect 自身疏失）。「PR 範圍檢查」CI 腳本已上線（PR #5/#11）。

**待辦佇列（優先序）**：

**🎉 里程碑：M0 資料飛輪完整閉合（2026-07-08）**——匯入 → 看懂(框) → 整理(審核 approve/reject) → Label → 黃金集 → 重新校準 → 信賴度回看懂站變色。飛輪全鏈：端到端橋接(票-0009/10/11)、run 記錄(orchestrator/票-0012)、Taxonomy、審核狀態機(#3)、審核 API(票-0013)、校準回填(#5/票-0014)、整理站 UI(票-0015)。core 86 / app 54 / ui 25 測試綠。

**🎉 里程碑：第一個真實老師上線（2026-07-08）**——OpenAI 雲端 VLM（票-0017，PR #24 `2f52b2e`）實機對 cat 圖畫出 bbox。含 sidecar 防孤兒（父死自殺）＋provider 錯誤回 CORS 502（不再裸 Failed to fetch）＋base_url 釘官方端點（防金鑰外洩）。金鑰放 gitignore 的 `provider-config.json`，預設 fixture（離線優先 §7.3）。一鍵啟動器 `start-visionforge.bat` 已納管（PR #25 `6efd815`，`--check` 通過）。**M0 現在用真老師跑得起來。**

1. ✅【已完成】資料集匯出（PR #22/#23）＋第一個真 provider OpenAI（PR #24）＋一鍵啟動器（PR #25）。
2. 【決策者拍板】下一個真實 provider（依 R2 §6.3「先接三個」）：本地開放詞彙（Grounding DINO/OWLv2，離線）或第二家雲端。**接滿 3 個才固化 provider invoke 介面**（A10，另開 ADR）。
3. 【Architect】試鏡引擎（黃金集實測→路由資格）→ Orchestrator v1（多 provider 成本路由）。
4. 【M1 鑄造廠】訓練（三檔預設）＋體檢報告＋部署包（R2 M1，確定性高，可蒸餾出離線學生模型）。
- 追蹤小事：CSP 既存 `ws:` 較寬、CORS `null` 原點——日後硬化（Electron↔sidecar 共享密鑰標頭）；L1 抽審積欠（Codex 迄今零瑕疵，補抽一張）。

## 四、環境怪癖（血淚清單，全部真實發生過）

1. **沙箱 git 不可用**（掛載層不支援 unlink／lock，會寫壞 config）→ git 一律由使用者或 Builder 在 Windows 端執行，你只給複製貼上的指令區塊。
2. **掛載同步會截斷檔案**（已發生 3 次：測試檔尾端、`contracts/__init__.py`、`pyproject.toml`——都斷在中間）→ 關鍵設定與程式檔用 **bash heredoc 直寫**；任何 Write／Edit 後用 bash 驗證（`tail`／`wc`／`ast.parse`）；**每輪結尾跑完整測試**。
3. 沙箱 Python 3.10 vs 專案目標 3.12 → 程式碼保持 3.10 可執行（ruff 已 ignore UP017／UP042，理由在 pyproject 註解）；別讓 ruff --fix 把它們改回 3.11+ 語法。
4. pip 需 `--break-system-packages`；pytest 前清 `__pycache__` 並用 `-B -p no:cacheprovider`（掛載 mtime 不可靠會吃到舊 bytecode）。
5. 使用者主控台為 cp950，git 輸出中文會亂碼——屬顯示問題，commit 本體正常，別誤判。
6. Windows 端可能有未提交工作樹變更；提交時一律 `git add` 指定檔案，不用 `git add -A`，除非 Architect 明確給出該指令並承擔範圍。

## 五、工作慣例（使用者已習慣，不要改）

1. **使用者不入場原則**：他只做兩件事——跑你給的指令、轉貼規格票給 Codex。不要請他讀內容或做技術判斷；介面出來前他不操作產品。
2. **每輪結尾必附「📋 下一步表」**：誰／做什麼／怎麼做，三欄。
3. 語言繁中、力求簡潔；commit 格式 `type(scope): 中文描述`。
4. **L1 抽審計數**：L0 已合併 1 張（票-0002，抽查通過）；下次抽查在第 3 張 L0 合併後。
5. ADR 採認模式：使用者已授權「**合併即視同採納**」（有異議他會說）；仍須在 ADR 狀態欄註明。
6. 治理三義務：新文件登記 `docs/README`、新名詞登記定義表、狀態變更同步——你自己也要遵守。
7. Builder（Codex）目前信任紀錄：兩張票零瑕疵；規格票缺欄它會退票，這是預期行為。

## 六、標準工作流

### Architect → 使用者

Architect 負責 ADR、規格票、合併策略與審查層級判斷。每輪結尾必給：

```text
誰／做什麼／怎麼做
```

如果需要 git 操作，Architect 必須給完整 PowerShell 指令，且列出要 stage 的檔案。不要要求使用者自行判斷「哪些檔案該進 commit」。

### 使用者 → Codex／Builder

使用者只轉貼票面：

```text
施工票-NNNN，票面在 docs/06-規格票/票-NNNN-標題.md。
分支開 codex/ticket-NNNN-短名。
只准動票面白名單。
```

Builder 施工完成後回報 commit、驗證、剩餘風險。L0 票 CI 綠即可依規則合併；L1/L2 依 Architect 指令。

### Builder → GitHub

無 remote 時先設定：

```powershell
git remote add origin https://github.com/AustinHongLee/VisionForge.git
git push -u origin main
git push -u origin codex/ticket-NNNN-短名
```

已有 remote 時只推當前需要的分支。未提交工作樹變更不會被 push，但會影響切分支與合併，操作前必先 `git status --short --branch`。

## 七、給接任模型的運行建議

| 工作類型 | 建議設定 |
|---|---|
| Kernel 設計（契約、統計、架構取捨） | Extended thinking 開高（預算從寬） |
| L2 審查、發票、ADR、治理 | 標準模式即可 |
| R2 等級的大型評審、修憲 | 最高 thinking 預算；必要時建議決策者用更高階模型做一次性檢視 |

暖機建議：就任後前一兩輪先做低風險工作（發票-0003、處理登記表），確認慣例吻合後再碰 kernel（校準引擎）。

---

*本手冊由卸任 Architect 維護。交接的哲學：**repo 就是 prompt**——所有決策在 ADR、所有規則在憲法、所有狀態在登記表，模型只是現任者（P3）。*
